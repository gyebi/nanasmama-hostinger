{
  "name": "nanasmama",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "manage:sitemap": "node scripts/generate-sitemap.mjs",
    "prebuild": "npm run manage:sitemap",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.19"
  },
  "dependencies": {
    "firebase": "^12.12.1"
  }
}
